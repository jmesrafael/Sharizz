# Frontend (Vite) — public, never put secrets here
VITE_API_BASE_URL=http://localhost:8787

# Worker secrets — set with `npx wrangler secret put NAME`, never commit real values
# SESSION_SECRET is used to sign short-lived room session tokens (HMAC).
SESSION_SECRET=replace-with-a-long-random-string
